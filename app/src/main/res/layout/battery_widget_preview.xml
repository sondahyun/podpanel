<?xml version="1.0" encoding="utf-8"?>
<!--
    Shown in the widget picker only. Glance renders the real thing, but the picker needs a
    static layout it can inflate without running our code.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center"
    android:padding="12dp">

    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_pods"
        android:contentDescription="@null" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:text="@string/widget_preview_values"
        android:textSize="15sp" />
</LinearLayout>
